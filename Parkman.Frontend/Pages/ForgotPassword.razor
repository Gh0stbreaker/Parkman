@page "/forgot-password"
@using Microsoft.AspNetCore.Components.Forms
@inject Parkman.Frontend.ViewModels.ForgotPasswordViewModel VM

<div class="container py-4">
    <h3 class="text-center mb-4 h4">Forgot Password</h3>

    <EditForm EditContext="VM.EditContext" OnValidSubmit="VM.HandleAsync" class="col-md-6 mx-auto card p-4">
        <DataAnnotationsValidator />
        <Forms.ValidationSummary />
        <div class="mb-3">
            <label class="form-label">Email</label>
            <InputText @bind-Value="VM.Model.Email" class="form-control" />
            <ValidationMessage For="@(() => VM.Model.Email)" class="text-danger text-sm" />
        </div>
        <button type="submit" class="btn btn-primary w-100" disabled="@VM.IsSubmitting">
            @(VM.IsSubmitting ? "Sending..." : "Send")
        </button>
    </EditForm>
    @if (VM.Message != null)
    {
        <p class="text-center mt-3">@VM.Message</p>
    }
</div>
